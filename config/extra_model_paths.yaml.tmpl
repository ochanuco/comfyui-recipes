# Rendered by scripts/render-extra-model-paths.py into .local/ComfyUI/extra_model_paths.yaml
managed_assets:
  base_path: __ASSET_ROOT__
  checkpoints: checkpoints
  clip: clip
  clip_vision: clip_vision
  configs: configs
  controlnet: controlnet
  diffusers: diffusers
  diffusion_models: diffusion_models
  embeddings: embeddings
  ipadapter: ipadapter
  loras: loras
  style_models: style_models
  text_encoders: text_encoders
  unet: unet
  upscale_models: upscale_models
  vae: vae
  vae_approx: vae_approx

# Add more groups if you keep models outside this repository root.
