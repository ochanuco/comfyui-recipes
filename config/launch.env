# Default bind address and port for `./scripts/run.sh`.
COMFYUI_HOST=127.0.0.1
COMFYUI_PORT=8188

# Optional extra CLI flags appended after host and port.
# Example: COMFYUI_EXTRA_ARGS="--disable-auto-launch --cuda-device 0"
COMFYUI_EXTRA_ARGS=
