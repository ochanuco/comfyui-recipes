# fall

Going over spectacularly. ONE fall tag.

```
(solo:1.5), (falling:1.5), (flailing:1.35), (surprised:1.35),
(open mouth:1.3), (motion lines:1.25), (outstretched arms:1.3),
(from above:1.25), full body
```

tripping + falling + fallen down together drew two figures on three seeds of
three -- one still upright, one already on the ground. They are three
moments, not three descriptions of one moment, and the model resolved that
by giving each moment a body. Competing tags did hold each other in place
for the socks, but that was two lengths of one garment at one instant;
carrying the idea over to a sequence was a bad generalisation.

`falling` is the one kept: mid-air is what reads as spectacular, and both of
the others describe ground contact. `flailing` supplies the drama and
`motion lines` is a comic convention that survives flat colour. Eight tags
after `(solo:1.5)`, and `closed mouth` comes out of FACE for the shout.

Inline note in the pose block, ahead of `(open mouth:1.3)`: no `(spread
legs)` here. Paired with `(from above)` that is exactly the crotch-forward
low-angle framing this project already threw a whole composition away over.

## Record

Canvas `(1024, 1536)`, `own_eyes=True`, `open_mouth=True`.

No further edits are recorded on this pose's record entry beyond those
parameters.
