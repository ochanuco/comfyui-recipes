# portrait

Head-and-shoulders portrait, close-up, face focus, smug with half-closed
eyes.

```
(solo:1.5), (portrait:1.5), (head and shoulders:1.4), (close-up:1.2),
(face focus:1.3), (smug:1.35), (half-closed eyes:1.3)
```

No experiment-history comment is attached to the `POSES` block itself for
this pose.

## Record

Canvas `(1024, 1024)`, `own_eyes=True`, `framing="head"`.

Head framings sit in a square: `(portrait:1.5)` alone lost to a 1024x1280
canvas and drew down to the thighs. See `shared.md` for the general square
vs. tall canvas story that other head framings (`allnighter`, `dizzy`,
`snarl`, `tehe`, `brush`, `hige`, `beg`, `sly`, `cackle` -- see their own
files) also apply.

`character_edits` carries the shared `_CRISS_CROSS` edit (see `shared.md`).
Note attached to it: 「肩紐がないね」 -- in frame here and never drawn until
the splice arrived. `e4ff4f8a`, picked with the cardigan up.
