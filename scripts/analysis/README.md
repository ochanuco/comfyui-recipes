# Analysis utilities

These are diagnostic and manual image-analysis/post-processing utilities.
They are not application entry points and do not queue recorded generations.
Run one explicitly when needed:

```bash
uv run scripts/analysis/<name>.py
```
